services:
  hackadashs_db:
    image: postgres:16
    container_name: hackadashs_db
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - hackadashs_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - hackadashs_net

  redis:
    image: redis:7-alpine
    container_name: hackadashs_redis
    ports:
      - "6379:6379"
    networks:
      - hackadashs_net

  hackadashs_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hackadashs_api
    env_file:
      - .env
    depends_on:
      hackadashs_db:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "${DJANGO_PORT}:8000"
    volumes:
      - ./src:/app
    command: sh -lc "python manage.py migrate && python manage.py bootstrap_hospital_data && python manage.py runserver 0.0.0.0:8000"
    networks:
      - hackadashs_net

  celery_worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hackadashs_celery_worker
    env_file:
      - .env
    depends_on:
      hackadashs_db:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./src:/app
    command: sh -lc "celery -A config worker -l info"
    networks:
      - hackadashs_net

volumes:
  hackadashs_postgres_data:

networks:
  hackadashs_net:
    driver: bridge